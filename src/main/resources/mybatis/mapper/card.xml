<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.mbcit.vivere.dao.CardDAO">

	<select id="getCardListByConsumerId" parameterType="int" resultType="cardVO">
		select * from card where consumerId = #{consumerId}
	</select>
	
	<select id="getCardByConsumerId" parameterType="int" resultType="cardVO">
	    select * from card where consumerId = #{consumerId}
	</select>
	
	<insert id="insertCard" parameterType="cardvo">
	    insert into card (
	        consumerId, pw, cardNum, cvc, validDate, bankName, createDate
	    ) values (
	        #{consumerId}, #{pw}, #{cardNum}, #{cvc}, #{validDate}, #{bankName}, now()
	    )
	</insert>    
	
<update id="updateCard" parameterType="cardvo">
    update card
    set
        pw = #{pw},
        cardNum = #{cardNum},
        cvc = #{cvc},
        validDate = #{validDate},
        bankName = #{bankName}
    where id = #{id}
</update>
</mapper>





